describe('patientService', function() {

  beforeEach(module('patients'));

  it('should ...', inject(function(patientService) {

    //expect(patientService.doSomething()).toEqual('something');

  }));

});

describe('flashService', function() {

  beforeEach(module('patients'));

  it('should ...', inject(function(flashService) {

    //expect(flashService.doSomething()).toEqual('something');

  }));

});

describe('spreadsheetService', function() {

  beforeEach(module('patients'));

  it('should ...', inject(function(spreadsheetService) {

    //expect(spreadsheetService.doSomething()).toEqual('something');

  }));

});
